#include <stdio.h>
#include <string>
#include <iostream>

using namespace std;

int main()
{
    string s;
    cin >> s;
    cout << s.size() << endl;

    // char *s1;
    // scanf("%s", s1);
    // int len = 0;
    // for (int i = 0; s1[i]; i++)
    // {
    //     len++;
    // }
    // printf("%d\n", len);

    // char *s2;
    // scanf("%s", s2);
    // int len = strlen(s2);

    // printf("%d\n", len);
    return 0;
}